interface:
  display_name: "Proteus"
  short_description: "Analyze and render protein structures"
  default_prompt: "Use $proteus to inspect this structural biology task, choose the right local toolchain, and return a concise analysis plus any generated outputs."

policy:
  allow_implicit_invocation: true
