interface:
  display_name: "Proteus"
  short_description: "Analyze and render protein structures"
  default_prompt: "Use $proteus to run a readiness check when needed, resolve structure inputs to local files with provenance, choose the right structural biology toolchain, and return concise analysis plus any generated outputs."

policy:
  allow_implicit_invocation: true
